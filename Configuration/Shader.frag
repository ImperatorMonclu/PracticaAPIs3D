#version 330
in vec4 vertexColor; // Entrada del color del vértice
out vec4 fragColor; // Salida del color del fragmento

void main() {
	fragColor = vec4(1.0f, 1.0f, 1.0f, 1.0f); // Cambia el color a blanco
}
